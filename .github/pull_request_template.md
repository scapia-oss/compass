## What

<!-- One or two sentences: what changed and why. Link related issues with "Closes #123". -->

## Type of change

<!-- Check the one that applies. -->

- [ ] Bug fix (non-breaking change that fixes an issue)
- [ ] New feature (non-breaking change that adds functionality)
- [ ] Improvement (refactor, performance, developer experience)
- [ ] Documentation (no code change)
- [ ] Breaking change (fix or feature that would cause existing functionality to change)

## What changed

<!-- Bullet list of specific changes. For skill/rule changes, describe the user-visible behavior difference. For builder changes, describe the generated output difference. -->

-

## How to test

<!-- Steps a reviewer can follow to verify the change works. -->

1.

## Checklist

- [ ] `npm run ci` passes locally (from `tools/cc-sdd/`)
- [ ] No `plugin/` directory committed — it is generated by CI to the `release` branch
- [ ] No `node_modules/`, `dist/`, or `coverage/` committed
- [ ] PR title follows conventional format: `type: short description` (e.g. `fix: handle empty spec dir`)
- [ ] If this changes user-facing behavior: noted in PR description for docs update
